# HPF Canonical Presentation Package — Volume I
## Master Canonical Framework
### Consolidated Final Package State — Updated Epoch 2026-03-20

**Role of this volume:** top-level canon. This is the first file to hand to a reader. It consolidates the enriched master canon with the later March 19 and March 20 updates that promoted the theorem track, repaired the reversible registry, replaced raw predecessor burden with continuation ambiguity in the live executable line, and recalibrated the bridge so the four-regime derived-$\zeta$ structure is recovered again under the current v0.2.8 executable line.

**What this volume does not do by itself:** it does not carry the full microscopic executable lineage or the full provenance map. Those remain the job of Volume II and Volume III.

---

# Part I. Executive Canonical Statement

## 1. What HPF is

The Hylo Phase Framework (HPF), previously presented as the Holographic Projection Framework (HPF), is a **regulatory execution framework** for bounded physical evolution on a finite-capacity substrate.

Its role is to:

1. Enforce hard execution constraints.
2. Track bounded substrate load.
3. Distinguish legality from validity.
4. Govern expert routing and lawful handoff.
5. Determine when effective theories remain trustworthy.
6. Route execution to deeper substrate-side authority when effective descriptions cease to be valid or legal.

In operating language: **HPF is the physics OS.**

## 2. What MDEA is

MDEA is the **routing kernel** inside HPF. It evaluates admissible experts, scores them, and assigns local descriptive authority.

## 3. What geometry/gravity is

Geometry/gravity is a **downstream effective expert regime**. It is:
* Downstream of HPF.
* Downstream of MDEA.
* Subordinate to legality constraints.
* Valid only in a bounded coarse-grained regime.
* Required to hand off when validity margin is lost.

## 4. What QPRCA is

QPRCA is the **deeper substrate expert / handoff destination** used when effective geometry is no longer valid or when legality/hard gates require deeper substrate-side execution.

## 5. Current one-sentence package status

HPF/MDEA is now a layered, truth-status-controlled regulatory execution framework with a locked legality/validity split, a locked two-wall architecture, an internally structured closure-strain quartet, a locked routing score/picker architecture, an architecturally derived $M_{\rm route}$, a repaired reversible executable registry, a live continuation-ambiguity microscopic burden channel, and a tail-sensitive v0.2.7 bridge-recalibrated derived-$\zeta$ recovery line, while strongest-form microscopic legality closure and first-principles geometry-validity closure remain open.

---

# Part II. Fixed Dependency Order

The canonical layer order is fixed:

1. **HPF** = regulator / legality framework
2. **MDEA** = routing kernel
3. **geometry / gravity** = effective expert regime
4. **QPRCA** = deeper substrate expert / handoff destination

This order must not be inverted.

**Anti-drift rule:**
* HPF is not an object-level gravity theory.
* Geometry is not the regulator.
* QPRCA is not the regulator; it is a substrate-side expert governed by HPF.
* Legality and validity must not be collapsed.

---

# Part III. Truth-Status Discipline

The package uses the following status ladder:
* **Fundamental**
* **Effective**
* **Derived**
* **Empirical / simulation-based**
* **Internally derived**
* **Open**

**Editorial rule:**
* Internally derived results must not be downgraded into merely fitted language.
* Candidate-locked architecture must not be inflated into strongest-form closure.
* Stale wording must not erase later closure already achieved.

---

# Part IV. Primitive Ontology and Core Axioms

## 6. Core axioms

The framework currently treats the following as foundational:
* Finite capacity
* Finite resolution
* Locality
* Reversibility
* Conservation

Bounded local update bandwidth is represented by:

$$0 \le \Lambda \le 1$$

At effective level,

$$\nabla_\mu \Lambda^{\mu\nu} = 0$$

## 7. Primitive substrate object

The primitive substrate object is the bounded dimensionless substrate load tensor:

$$\Lambda^{(\mathrm{sub})}_{\mu\nu}(x)$$

**Canonical component meanings:**
* $\Lambda_{00}=\Lambda_0$: temporal capacity depletion.
* $\Lambda_{0i}$: directional load flow.
* $\Lambda_{ij}$: anisotropic transport load.

A key downstream viability observable is:

$$G_{\rm health}=1-\Lambda_0$$

but this is not the regulator and not a substitute for microscopic legality.

---

# Part V. Legality, Validity, and the Two-Wall Architecture

## 8. Legality

A state/evolution is **legal** if it satisfies substrate constraints:
* Bounded load
* Finite resolution
* Locality
* Reversibility
* Closure of the relevant update structure

## 9. Validity

A theory/expert is **valid** only if it remains trustworthy as an effective description inside a still-legal region.

Therefore:
* Legality may survive while geometry-validity fails.
* HPF must be able to route away from geometry before literal substrate saturation.
* Legality and validity are not the same threshold and not the same diagnostic.

## 10. Locked two-wall structure

Two walls remain distinct:

### 10.1 Exact substrate hard wall
$$\Lambda_c^{(\mathrm{sub})}=1$$

### 10.2 Lower geometry-validity wall
$$\Lambda_c^{(\mathrm{geom})}<1$$

**Canonical meaning:**
* Geometry may become invalid while substrate legality still survives.
* This invalidity must occur only in a bounded pre-illegality band.
* Full substrate illegality remains the separate upper-wall event.

## 11. Concise two-wall bridge form

The current concise downstream bridge stack is:

$$U_{\rm geom}^{(\rm NL)}=1-(1-U)(1-1.09\,m_{\rm loop})(1-1.25\,m_{\rm route})$$

$$\Lambda_{\rm geom}=\mathrm{clip}\!\left[U_{\rm geom}^{(\rm NL)}(1+\rho_F F)-\rho_H H\right]_{[0,1]}$$

$$\chi_{\rm geom}^{*}=\mathrm{clip}\!\left[\frac{\Lambda_{\rm geom}-\lambda_{\rm soft}}{\lambda_{\rm hard}-\lambda_{\rm soft}}\right]_{[0,1]}$$

**Canonical provisional parameters retained in the package:**
$$\rho_F=0.6, \quad \rho_H=0.25, \quad \lambda_{\rm soft}=0.50, \quad \lambda_{\rm hard}=0.72$$

The canonical split remains:
* **State renormalization** $\to \zeta$
* **Handoff escalation** $\to \chi^*_{\rm geom}$

### 11.1 Current executable support-model refinement

The v0.2.7/v0.2.8 executable line sharpened the coarse lower-wall proxy by adding legality-tail sensitivity rather than relying on mean legality pressure alone. The active coarse proxy can be written as:

$$F_{\rm bridge} = \mathrm{clip}\!\left[U + 0.35\,\nu_F + 0.50\,\overline{(F-1)_+} \right]_{[0,1]}$$

where $\nu_F$ is the fraction of local blocks with $F_{\rm block} \ge 1$ and $\overline{(F-1)_+}$ is the mean excess above the hard legality wall. This does **not** collapse the lower wall into the upper wall; it is a downstream coarse support-model correction showing that legality-tail structure matters when the registry is made reversibly honest.

### 11.2 Canonical interpretation of the current lower-wall support

The March 20 executable correction supports the theorem-safe claim that geometric validity can fail before microscopic illegality and that a mean-only coarse bridge is too lossy to preserve the hard-edge distinction. It does **not** yet constitute a unique first-principles derivation of the lower-wall constitutive law.

---

# Part VI. Candidate Microscopic Legality Structure

## 12. Candidate microscopic legality gate

$$L_{\rm QPRCA}(x,t)=1 \iff F(x,t)<1$$

Failure occurs at $F(x,t) \ge 1$. $F$ is substrate-side. It is **not** the geometry-validity score, not the routing kernel, and not the handoff signal.

## 13. Five-channel candidate burden structure

The current canon still supports the five-channel burden basis:
1. Channel occupancy $C$.
2. Blocked transport fraction $B$.
3. Reservoir / relaxation burden $R$.
4. Directional anisotropy $A$.
5. Reversible-update closure deficit / microscopic continuation-ambiguity burden.

**Historical note:**
* The current canon should read that channel more carefully as a **local operator-selection / continuation-ambiguity burden** once the registry is fully reversible.
* The executable variable may still be denoted $P$ for continuity, but canon-level prose should no longer treat it as raw predecessor count.

---

# Part VII. Closure-Strain Engine and the Demand-Side Quartet

The distinguishability demand remains structurally live:

$$M_{\rm req}=M_{\rm coll}+M_{\rm amb}+M_{\rm loop}+M_{\rm route}$$

* $M_{\rm coll}$: collision burden.
* $M_{\rm amb}$: predecessor/path ambiguity burden.
* $M_{\rm loop}$: short-cycle closure burden.
* $M_{\rm route}$: forward continuation / destination assignment burden.

Live successor supports are formed through admissibility and continuation fitness:

$$\omega_r=L_rV_rR_r\,W_r\,\zeta^{p_r}(1-\chi_r)^{q_r}M_r$$

with

$$\pi_r=\frac{\omega_r}{\sum_{r'}\omega_{r'}+\varepsilon}, \quad N_{\rm eff}^{(\rm route)}=\frac{1}{\sum_r \pi_r^2+\varepsilon}, \quad A_{\rm route}=1-\max_r \pi_r$$

leading to

$$M_{\rm route}=\mu_{\rm route}\,\sigma_{\rm route}\,A_{\rm route}\,\log N_{\rm eff}^{(\rm route)}$$

---

# Part VIII. Geometry / Gravity Effective Expert

Geometry/gravity remains a regime-limited effective expert. The package still supports:
* Coarse-grained load as geometry source.
* Load-to-stress-energy normalization.
* Weak-field and Einstein-like closure sectors.
* Rotating-source/Kerr-like asymptotic sector.
* Explicit geometry-validity burden and routing discipline.

A retained bridge relation is:

$$T_{\mu\nu}=\frac{\hbar}{a^3\Delta t}\,\Lambda_{\mu\nu}$$

---

# Part IX. Derived-$\zeta$ Recovery Is Now Official Canonical Scope

## 14. Why this sector is now explicit

The older framework’s phenomenological strength was a **smooth calibrated corridor**. The newer rewrite is now judged successful only if it can recover the old smooth corridor as a **derived** output.

Correct relationship:
$$\text{current canon} \to \zeta_{\rm derived}$$

## 15. Current live bridge

The package should now be read with the live bridge:
$$(M_{\rm coll},M_{\rm amb},M_{\rm loop},M_{\rm route}) \to U \to U_{\rm geom}^{(\rm NL)} \to \Lambda_{\rm geom} \to \chi_{\rm geom}^{*}, \quad F \to L_{\rm QPRCA}$$

followed by a derived projection:
$$\zeta_{\rm derived}=\mathcal Z\!\left(F,U,U_{\rm geom}^{(\rm NL)},\Lambda_{\rm geom},\chi_{\rm geom}^{*}\right)$$

## 16. Consolidated executable progression in the derived-$\zeta$ sector

### v0.2.2
Grouped-$L_2$ promotion:
$$F_{\rm grp-L2}= \frac{\sqrt{C^2+A^2}+P+\sqrt{B^2+R^2}}{M_{\rm free}}$$

### v0.2.4
Floor-fixed piecewise anchored projection:

$$
S_{\rm eff}=
\begin{cases}
S_{\rm floor}+(S_{\rm onset}-S_{\rm floor})\left(\frac{S_{\rm raw}}{S_{\rm raw,onset}}\right)^{p_{\rm low}}, & S_{\rm raw}\le S_{\rm raw,onset} \\[1.2ex]
S_{\rm onset}+(S_{\rm hard}-S_{\rm onset})\left(\frac{S_{\rm raw}-S_{\rm raw,onset}}{1-S_{\rm raw,onset}}\right)^{p_{\rm high}}, & S_{\rm raw}>S_{\rm raw,onset}
\end{cases}
$$

**Current tuned values:**
* $S_{\rm floor}=0.45$
* $S_{\rm onset}=1.05$
* $S_{\rm hard}=5.7889$
* $S_{\rm raw,onset}\approx 0.19813648$
* $p_{\rm low}=0.78$
* $p_{\rm high}=11.06$

---

# Part X. Theorem and Reproduction Track

## 18. Current theorem targets

### Primary theorem target: HPF Two-Wall Regulator Theorem

In a bounded finite-resolution reversible substrate with limited local execution capacity, microscopic legality and downstream effective-theory validity are in general distinct constraints; therefore a lawful regulator must permit a bounded pre-illegality band in which effective geometry becomes invalid before full microscopic illegality.

### Secondary theorem target: Microscopic Legality Sufficiency Theorem

The old naive form $F<1 \Rightarrow \exists \hat T$ is no longer acceptable. The work-chain now includes Selection Lemmas and Operator-Richness sharpening.

---

# Part XI. Current Open Frontiers

Main frontier items:
* Final QPRCA operator grammar.
* Strongest-form microscopic closure of $F$.
* Full first-principles lower-wall derivation.
* One discriminating external prediction.

---

# Part XII. Practical Reading Rule

If sending only one file, send this one.
If the reader needs to see the work rather than only the result, send Volumes I–III together.

