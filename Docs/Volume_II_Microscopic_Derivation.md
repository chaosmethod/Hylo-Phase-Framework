# HPF Canonical Presentation Package — Volume II
## Microscopic Derivation, Executable Development Line, and Theorem-Work Spine
### Consolidated Rewrite — Milestone Lock State (2026-04-09)

**Role of this volume:** This volume carries the microscopic derivation line, the executable substrate-model progression, the grouped-$L_2$ legality branch, the March 20 reversible-registry and continuation-ambiguity corrections, the v0.2.7 bridge recalibration, and the active theorem-work chain for microscopic legality. It also updates the microscopic package to the April 2026 milestone state: the 4-bit site alphabet $(n_L,n_R,b,q)$ is no longer described as provisional “toy” scaffolding, but as the candidate-locked fundamental operator grammar of the substrate, with exactly 15 active non-vacuum microstates per site.

**What this volume does not claim:** It does not claim strongest-form microscopic closure is finished. The deepest target remains an existence statement over legal local bounded bijections. The point of this volume is to state exactly what the executable line has established, exactly what has been promoted, and exactly what is still open.

---

# 1. Executive consolidation outcome

The microscopic package now separates cleanly into four layers.

## Tier A — framework authority above the microscopic line
- Volume I of this package.
- The active canonical layer map HPF $→$ MDEA $→$ geometry / gravity expert $→$ QPRCA.
- The live Symbol Index.

## Tier B — current microscopic synthesis and working status
- This consolidated Volume II.
- The current executable substrate-model family through v0.2.7.
- The bridge stack linking microscopic burden to downstream validity diagnostics.

## Tier C — live theorem-work notes
- Corrected Selection Lemma.
- Continuation-ambiguity reformulation of the ambiguity burden.
- Capacity-Margin Lemma.
- Aggregation-Law Adequacy Lemma.
- Operator-Richness Lemma.

## Tier D — executable history and calibration artifacts
- v0.1 through v0.1.3 historical legality line.
- v0.2.0 through v0.2.7 enriched-canon executable line.
- The current bridge recalibration outputs and JSON artifacts.

The package should therefore be read in this order: framework authority first, microscopic executable line second, theorem-work third, historical calibration material fourth.

---

# 2. Microscopic program status that remains fixed

The substrate-side legality target remains:

$$F(x,t)=\frac{M_{\mathrm{req}}^{(\mathrm{micro})}(x,t)}{M_{\mathrm{free}}^{(\mathrm{micro})}(x,t)}, \qquad L_{\mathrm{QPRCA}}(x,t)=1 \iff F(x,t)<1$$

At deepest intended form, this is an existence statement over legal local bounded bijections. That strongest form is still open, but the target is sharp.

The local executable line utilizes the **candidate-locked 4-bit site alphabet** $(n_L,n_R,b,q)$. This is no longer treated as a provisional toy matrix, but as the exact fundamental state-space grammar of the substrate within the active microscopic package, generating exactly 15 active non-vacuum microstates per site. The line features reversible block-rule structure and exact predecessor counting inside the tested family.

Two consequences are now fixed:

1. the executable line is not merely a disposable demo language; it is the active local grammar through which the substrate program is currently expressed,
2. the grammar promotion does **not** by itself prove strongest-form closure; it sharpens what the executable line is now legitimately allowed to represent.

---

# 3. What the candidate-locked grammar now means

## 3.1 Local state space

The site alphabet is

$$(n_L,n_R,b,q)$$

with one bit each for left occupancy, right occupancy, block/boundary status, and queue/state flag. This gives:

$$2^4 = 16$$

local configurations.

Because the all-zero vacuum state

$$(0,0,0,0)$$

is not an active information-carrying excitation state, the active local microstate count is:

$$16 - 1 = 15.$$

This count is now canonically important. It is not merely a coding convenience. It is the active microscopic count used by the April 2026 milestone lock, including the closure of Obligation 1 in the fine-structure candidate note via

$$N_{\rm cycle}=24 \times 15 = 360.$$

This licenses the baseline count for cross-volume substrate accounting. It does not by itself promote the full fine-structure lane into canon; the second and third terms, and the claim that both derivational obligations are closed, remain candidate-note level rather than canon-level status.

## 3.2 What has and has not been promoted

Promoted:
- the 4-bit alphabet itself,
- the exact active microstate count,
- the legitimacy of using this grammar in cross-volume substrate accounting.

Not yet promoted:
- proof that this is the unique admissible substrate grammar,
- proof that the present finite reversible registry is already the final strongest-form local operator family,
- proof that every microscopic burden term has reached constitutive closure.

So the correct reading is: **the grammar is candidate-locked; the full microscopic closure program remains open.**

---

# 4. What the v0.1.x line established

## 4.1 v0.1 — first executable legality harness
First executable legality harness.

**Representative baseline:**
- Additive aggregation.
- $M_{\mathrm{cap}}=4$, $\mu_P=1.0$.
- Acceptance $=0.25$.
- Mean $F_{\mathrm{block}} \approx 1.32157$.
- Mean predecessor count $=6.0$.
- Exact total-load conservation.

## 4.2 v0.1.1 — predecessor multiplicity became structure-sensitive
Applicable-rule predecessor algebra made predecessor multiplicity variable.

**Observed predecessor histogram:**
- $6 \to 120$ outputs.
- $7 \to 100$ outputs.
- $8 \to 32$ outputs.
- $9 \to 4$ outputs.

Gain: predecessor burden became structure-sensitive.

Limitation: raw $\log_2|P|$ overweighted predecessor cost and dominated rejection.

## 4.3 v0.1.2 — normalized predecessor baseline
The normalized predecessor baseline remained:

$$P_{\mathrm{norm}}(O)= \frac{\log_2 |\mathcal{P}(O)|-\log_2 P_{\min}}{\log_2 P_{\max}-\log_2 P_{\min}}$$

**Observed structure:**
- $8 \to 64$ outputs.
- $9 \to 128$ outputs.
- $10 \to 64$ outputs.
- Bounds: $P_{\min}=8$, $P_{\max}=10$.

**Representative baseline:**
- Additive aggregation.
- $M_{\mathrm{cap}}=4$, $\mu_P=1.0$.
- Acceptance $\approx 0.4078125$.
- Mean $F_{\mathrm{block}} \approx 1.04536$.
- Mean predecessor count $\approx 9.1375$.
- Dominant rejection almost entirely collision-led.

Consolidated judgment: **v0.1.2 remains the cleanest balanced historical baseline.**

## 4.4 v0.1.3 — richer non-predecessor ecological branch
Refined non-predecessor channel branch.

**Representative baseline:**
- Additive aggregation.
- $M_{\mathrm{cap}}=4$, $\mu_P=1.0$.
- Acceptance $\approx 0.384375$.
- Mean $F_{\mathrm{block}} \approx 1.07922$.
- Mean predecessor count $\approx 9.0625$.
- Rejection ecology: $B=283$, $R=108$, $A=3$, $C=0$, $P=0$.

Judgment: v0.1.3 is a richer non-predecessor ecological branch, but not a full replacement for v0.1.2.

---

# 5. Why the package moved beyond v0.1.x

The enriched March 2026 rewrite imposed three requirements that the historical v0.1.x line did not yet satisfy together:

1. explicit legality / validity separation,
2. explicit geometry-validity bridge stack,
3. explicit derived-$\zeta$ recovery from the layered canon rather than a placeholder proxy.

That is why the package now treats v0.2.x as the live executable family.

The key point is not that v0.1.x failed. It is that v0.1.x did not yet carry the bridge structure the modern package requires.

---

# 6. Executable progression from v0.2.0 to v0.2.7

## 6.1 v0.2.0 — canonical enriched-canon executable
First executable reference aligned to Volume I logic. It carries legality/validity separation, the bridge stack, and the current-canon treatment of route burden.

## 6.2 v0.2.1 — first explicit derived-$\zeta$ recovery line
The immediate recovery map introduced an effective-flux axis:

$$S_{\mathrm{eff}}=1+w_U U+w_{U_g}U_{\mathrm{geom}}^{(\mathrm{NL})}+w_{\Lambda} \Lambda_{\mathrm{geom}}+w_{\mathrm{loop}}m_{\mathrm{loop}}+w_{\mathrm{route}}m_{\mathrm{route}}+w_F\Phi_F$$

with softened legality term $\Phi_F=6[U_{\mathrm{raw}}-0.85]_+$, followed by:

$$\zeta_{\mathrm{derived}}=1-\sigma(k(S_{\mathrm{eff}}-1.05)).$$

Purpose: keep legality vs validity separation intact while restoring a smooth summary output.

## 6.3 v0.2.2 — grouped-$L_2$ promotion
New live recommended combiner:

$$F_{\mathrm{grp-L2}}=\frac{\sqrt{C^2+A^2}+P+\sqrt{B^2+R^2}}{M_{\mathrm{free}}}$$

Interpretation: grouped-$L_2$ removed the immediate hard-collapse behavior produced by purely additive logic, and acceptance rose strongly.

## 6.4 v0.2.3 — tuned effective-flux projection
Replaced the linear map $S_{\mathrm{eff}}=S_{\mathrm{hard}}S_{\mathrm{raw}}$ with the power-compressed form:

$$S_{\mathrm{eff}}=S_{\mathrm{onset}}+(S_{\mathrm{hard}}-S_{\mathrm{onset}})S_{\mathrm{raw}}^{14}$$

with softer logistic slope $k=0.8$. This improved representation of the onset corridor but made **stable_localized** mathematically unreachable.

## 6.5 v0.2.4 — floor-fixed grouped-$L_2$ derived-$\zeta$ line
The piecewise anchored projection fixed the floor bug while preserving onset / corridor / hard ordering.

**Recovered target cases:**
- **Stable localized:** $S_{\mathrm{eff}} \approx 0.547986$, $\zeta \approx 0.599075$.
- **Onset corridor:** $S_{\mathrm{eff}}=1.05$, $\zeta=0.5$.
- **Validity corridor:** $S_{\mathrm{eff}} \approx 2.004378$, $\zeta \approx 0.317886$.
- **Hard saturation:** $S_{\mathrm{eff}}=5.7889$, $\zeta \approx 0.022072$.

## 6.6 Theorem-spine clarification after v0.2.4
After the floor fix, the theorem program was sharpened into an explicit spine including the corrected Selection Lemma, Capacity-Margin Lemma, and the Operator-Richness bottleneck.

## 6.7 v0.2.5 — reversible registry repair
Operator-richness analysis exposed a real registry defect: one named rule failed the bijection check. This was replaced with a reversible equal-queue toggle. The repair showed that the old predecessor-burden channel becomes trivial in a fully bijective finite registry if one only counts raw output multiplicity.

## 6.8 continuation-ambiguity redefinition of the ambiguity channel
The replacement channel shifts from **raw predecessor count** to **input-conditioned continuation ambiguity** over the reversible operator family.

Fix a local input block

$$\psi_{\mathrm{in}} \in \mathcal{S}_{\mathrm{loc}}$$

and a finite admissible reversible family

$$\mathcal{O}_{\mathrm{rev}}=\{\hat{T}_1, \dots, \hat{T}_N\}.$$

Two outputs are assigned to the same continuation class when $\Sigma(O_i)=\Sigma(O_j)$. The effective continuation multiplicity is:

$$N_{\mathrm{eff}}^{(\mathrm{cont})}(\psi_{\mathrm{in}})=\frac{1}{\sum_\kappa p_\kappa^2}$$

and the normalized ambiguity burden is:

$$P_{\mathrm{cont}}(\psi_{\mathrm{in}})=\frac{\log N_{\mathrm{eff}}^{(\mathrm{cont})}(\psi_{\mathrm{in}})}{\log N_{\mathrm{eff,max}}^{(\mathrm{cont})}+\varepsilon}.$$

## 6.9 v0.2.6 — continuation-ambiguity executable patch
The executable line was patched so the active $P$-channel measures continuation ambiguity. This made the ambiguity channel live again under a fully reversible registry.

## 6.10 v0.2.7 — bridged continuation-ambiguity line
The current executable correction recalibrates the bridge by adding legality-tail information:

$$F_{\mathrm{bridge}} = \mathrm{clip}\!\left[ U +0.35\,\nu_F +0.50\,\overline{(F-1)_+} \right]_{[0,1]}$$

Current anchor values:
- **Stable localized:** $S_{\mathrm{eff}}=0.600637$.
- **Onset corridor:** $S_{\mathrm{eff}}=1.050000$.
- **Validity corridor:** $S_{\mathrm{eff}}=2.212534$.
- **Hard saturation:** $S_{\mathrm{eff}}=5.788900$.

Consolidated judgment: **v0.2.7 is the current active enriched-canon executable reference line.**

---

# 7. The corrected theorem-work chain

## 7.1 Corrected Selection Lemma
The corrected object is

$$F_{\min}(\psi_{\mathrm{in}})=\min_{\hat{T}\in\mathcal{O}_{\mathrm{QPRCA}}} F(\psi_{\mathrm{in}};\hat{T}).$$

Corrected statement:

$$F_{\min}(\psi_{\mathrm{in}})<1 \Rightarrow \exists \hat{T}\in\mathcal{O}_{\mathrm{QPRCA}}\text{ with }F(\psi_{\mathrm{in}};\hat{T})<1.$$

This is the non-circular microscopic selection statement. It is weaker than strongest-form closure, but it is theorem-safe.

## 7.2 Historical predecessor-pathology evidence
Evidence supported the weaker claim that worst-form universal predecessor saturation was not observed in the tested finite family. This is now a historical diagnostic rather than the active theorem target.

## 7.3 current theorem-safe ambiguity reformulation
The theorem-facing question is now: for a given local input, does the reversible operator family collapse effectively to one continuation class, or does it retain a nontrivial continuation-ambiguity burden?

## 7.4 Capacity-Margin Lemma (draft)
Working claim: once universal predecessor trapping is excluded, remaining illegality near threshold is primarily a **margin problem**, not a pure selection impossibility.

## 7.5 Aggregation-Law Adequacy Lemma (draft)
Current sharp question: does the present aggregate law track genuine bounded reversible impossibility, or does it over-reject skewed but still executable local updates?

## 7.6 Operator-Richness Lemma (checkpoint status)
The current executable line now supports a sharper answer: the registry is finite and explicit, the minimizer is automatically attained, and the old non-bijective defect has been repaired. What remains open is whether the present reversible family is rich enough to support the strongest intended microscopic legality claim.

## 7.7 resulting sufficiency-program wording
> The first microscopic legality lemma has been corrected into a non-circular selection form using $F_{\min}$. Preliminary executable sweeps indicate that worst-form predecessor saturation traps are absent in the current operator family, but continuation ambiguity and free-margin depletion remain capable of triggering rejection under tight local conditions.

---

# 8. How the microscopic line now interfaces with the rest of HPF

The microscopic line must now be read with four guardrails.

## 8.1 It is subordinate to Volume I
Volume I controls the layer order, status discipline, and legality / validity split. Volume II does not override that architecture.

## 8.2 It supports, but does not replace, Volume IV
The microscopic line is not the derivation source for the active vacuum-sector package. It provides substrate-side executable legitimacy and local grammar support. Volume IV remains the vacuum-branch derivation package.

## 8.3 It now materially supports the April 2026 milestone lock
Because the 4-bit grammar is promoted, the microscopic line now contributes directly to a live cross-volume count:

$$24\text{ spatial sectors} \times 15\text{ active microstates}=360.$$

That means microscopic grammar status is no longer a side issue. It participates in the active candidate chain.

## 8.4 It still does not prove uniqueness
Using the grammar in a live candidate chain does not prove there is no other admissible substrate grammar. The package must continue to distinguish **candidate-locked operational use** from **full uniqueness proof**.

---

# 9. Current best executable reference rule

- **Historical stable baseline:** v0.1.2.
- **Current active enriched-canon branch:** v0.2.7 bridged continuation-ambiguity line.
- **Current microscopic grammar status:** candidate-locked 4-bit substrate grammar $(n_L,n_R,b,q)$ with 15 active non-vacuum microstates.

---

# 10. Compatibility note on naming lag in source files

Some live source filenames, class names, and comments still retain older `MicroToy` wording for import stability and repository continuity. That naming lag is now purely compatibility residue.

The canonical reading is:
- the executable line is still an **executable substrate model**,
- the local grammar is **not** to be downgraded back into “toy only” language,
- strongest-form microscopic closure is **still open**.

So the correct package stance is neither inflation nor downgrade. The grammar is promoted; the closure target remains unfinished.

---

# 11. Immediate next technical work

1. Broader aggregation stress sweeps across grouped-$L_2$, additive, and other overlap-aware candidate combiners.
2. Broader v0.2.7 trajectory validation.
3. Constitutive sharpening of continuation-signature classes and operator-family richness.
4. Promotion of the current bridge recalibration toward a more principled lower-wall constitutive law.
5. Eventual source-level cleanup of residual `MicroToy` naming where doing so will not break imports or repository continuity.

---

# 12. Freeze wording

> The microscopic HPF/QPRCA line remains an executable substrate-model program whose legality target is $F(x,t)=M_{\rm req}^{(\rm micro)}/M_{\rm free}^{(\rm micro)}$ with $L_{\rm QPRCA}=1 \iff F<1$. The local 4-bit site alphabet $(n_L,n_R,b,q)$ is now candidate-locked as the fundamental state-space grammar of the substrate, yielding 16 local configurations and 15 active non-vacuum microstates per site. The historical v0.1.2 line remains the cleanest balanced baseline, while v0.2.7 is the current active enriched-canon executable reference through the bridged continuation-ambiguity law. This promotion does not complete strongest-form microscopic closure: the deepest target remains an existence statement over legal local bounded bijections, and operator-family richness, aggregation adequacy, and constitutive completion remain open.
